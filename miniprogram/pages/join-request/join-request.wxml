<view class="page">
  <view class="card">
    <view class="title">提交入队申请</view>
    <view class="field-label">姓名</view>
    <input class="form-input" placeholder="请输入姓名" placeholder-class="form-placeholder" value="{{form.name}}" data-field="name" bindinput="onInput" />
    <view class="field-label">手机号</view>
    <input class="form-input" type="number" placeholder="请输入手机号" placeholder-class="form-placeholder" value="{{form.phone}}" data-field="phone" bindinput="onInput" />
    <view class="field-label">场上位置</view>
    <input class="form-input" placeholder="前锋 / 中场 / 后卫 / 门将" placeholder-class="form-placeholder" value="{{form.position}}" data-field="position" bindinput="onInput" />
    <view class="field-label">球衣号码</view>
    <input class="form-input" type="number" placeholder="请输入球衣号码" placeholder-class="form-placeholder" value="{{form.jersey_number}}" data-field="jersey_number" bindinput="onInput" />
    <view class="field-label">备注</view>
    <textarea class="form-textarea" placeholder="可填写想说的话" placeholder-class="form-placeholder" value="{{form.message}}" data-field="message" bindinput="onInput" />
    <button class="primary" bindtap="submit">提交申请</button>
  </view>
</view>
